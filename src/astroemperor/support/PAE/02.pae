    # parameterisation == 2

    periods = theta[slice(0, b_len*kplanets, b_len)]
    amps = theta[slice(1, b_len*kplanets, b_len)]
    eccs = theta[slice(3, b_len*kplanets, b_len)]

    x = np.array([periods, amps, eccs])
